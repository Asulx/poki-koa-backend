[project]
name = "mamoru-backend"
version = "0.1.0"
description = "Backend para el sistema de monitoreo de cunas"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=5.2.17",
    "django-cors-headers>=4.9.0",
    "djangorestframework>=3.18.0",
]
